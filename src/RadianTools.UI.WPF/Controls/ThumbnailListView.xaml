<UserControl x:Class="RadianTools.UI.WPF.Controls.ThumbnailListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:RadianTools.UI.WPF.ViewModels"
             xmlns:wpftk="clr-namespace:WpfToolkit.Controls;assembly=VirtualizingWrapPanel"
             x:Name="root">
    <Grid>
        <ListBox x:Name="PART_ListBox"
                 ScrollViewer.CanContentScroll="True"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 BorderThickness="0"
                 Background="Transparent">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <wpftk:VirtualizingWrapPanel
                        Orientation="Horizontal"
                        SpacingMode="None"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                </Style>
            </ListBox.ItemContainerStyle>

            <ListBox.ItemTemplate>
                <DataTemplate DataType="{x:Type vm:ThumbnailItemViewModel}">
                    <Border Margin="2" Padding="2" BorderBrush="Black" BorderThickness="1">
                        <StackPanel>
                            <Image Source="{Binding Thumbnail}"
                                    Stretch="Uniform"
                                    Width="{Binding ElementName=root, Path=ThumbnailWidth}"                                       
                                    Height="{Binding ElementName=root, Path=ThumbnailHeight}"
                                    RenderOptions.BitmapScalingMode="HighQuality"/>

                            <TextBlock
                                    Margin="3,0,0,0"
                                    Text="{Binding DisplayName}"
                                    TextWrapping="Wrap"
                                    Width="{Binding ElementName=root, Path=ThumbnailWidth}"                                       
                                    Height="{Binding ElementName=root, Path=TextHeight}"
                                    ClipToBounds="True" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>